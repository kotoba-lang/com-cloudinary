{:paths ["src" "test"]
 :tasks {test {:doc "Run the cljc contract + behavioral test under babashka"
               :extra-paths ["test"]
               :requires ([cloudinary.main-test])
               :task (let [{:keys [fail error]} (clojure.test/run-tests 'cloudinary.main-test)]
                       (System/exit (if (pos? (+ fail error)) 1 0)))}}}
